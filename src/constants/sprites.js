const spriteDir = '/data/sprites';

const SPRITES = {
	phoneShell: {
		location: `${spriteDir}/PhoneShell.png`,
		config: {
			frameWidth: 1600,
			frameHeight: 800,
		},
	},
};

export default SPRITES;